# class 18


in function tool we can make a function tool is_enabled true or false also we can do tool choice none on seperate tool. this mean when we give falso to is enabled in function tool it stop the tool to be used by agent while on other hand when we give none to the tool choice it give the tool to agent but we are not allowing agents to use it. 

later we were passing false or true to the is_enabled while we can make a seperate function where we define True or False in the return and later in e give that function to the is_enabled so it should take the value from that function

GPT 4.1 CookBook
Prompt engeneering
diff btw CoT (Chain of thaughts) and ToT (Tree of thaughts)
platform.openai.com
